# job_submission_condor

Out-of-container supports for full simulation campaigns via PanDA.

## Usage

See `scripts/submit_csv.sh` for the PanDA submission entry point. This can be run on EIC nodes, e.g. eic07.
